#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main ( ) {
    cara 1
    char arr[100];
    int sum = 0;

    while (scanf("%s", arr) !=EOF) {
        sum += stoi(arr);
    }
    cout << sum << endl;


    // cara 2
    // int sum = 0;
    // for (int n = 0; cin >> n; sum+=n);
    // cout << sum << endl;

    // cara 3
    // vector<int> vNum;

    // int num;
    // while (cin >> num) {
    //     vNum.push_back(num);
    // }

    // int sum = 0;
    // for (int i = 0; i < vNum.size(); i++) {
    //     sum += vNum[i];
    // }

    // cout << sum << endl;
    return 0;
}

