#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef unsigned long long ull;

int main( ) {
int a, b;
scanf("%d %d", &a, &b);

int sum = a + b;
printf("%d", sum);

    return 0;
}