#include <bits/stdc++.h>
using namespace std;

int main () {
    ios::sync_with_stdio(0);
    int a, b ,c, k;
    // a b c angka
    // if k = 0 { besar ke kecil }
    // if k = 1 { kecil ke besar }

    cin >> a >> b >> c >> k;
    

    return 0;
}